// src/engine/quantities/constants.ts
export const MIN_CHUTE_MM = 50;
export const CUT_TOLERANCE_MM = 5;    // edge classification in buildCutTable (how close to boundary = factory edge)
export const REUSE_TOLERANCE_MM = 10; // offcut size matching in assignOffcuts (how much smaller can a chute be)
